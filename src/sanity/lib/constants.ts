/**
 * Sanity config (e.g. referenceable document types for links).
 * Used by schema types that need to link to pages and case studies.
 */

export const PAGE_REFERENCES = [{ type: 'page' }, { type: 'caseStudy' }]
