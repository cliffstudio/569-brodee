'use client'

import { useEffect } from 'react'

export default function ViewportDetection() {
  useEffect(() => {
    // In-viewport animation temporarily disabled for testing.
    // Leaving this effect empty keeps the component mounted as a no-op.
  }, [])

  return null
}
